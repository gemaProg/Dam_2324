package ProfesorAlumnos;

public class Profesor {
    public static double dameNota(){
        return Math.random()*11;
    }
    public static double areaCirculo(double radio){
        return Math.PI*Math.pow(radio,2);
    }
}
